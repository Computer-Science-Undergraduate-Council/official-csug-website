import timeline from './index.data'
import {TimeLineView} from './index.view'

//Timeline data has been exported as default without the view in case it can be used in something else
export default timeline;
export {TimeLineView};